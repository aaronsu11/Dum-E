# GR00T SO101 contract fixture

Metadata only, extracted from the validated GR00T-N1.7-3B-SO101 checkpoint. Statistics retain only `new_embodiment`; the empty shard index identifies the format without shipping weights. These fixtures exercise preprocessing/guard behavior, not checkpoint authenticity or model loading. Download and verify actual model artifacts separately for GPU integration tests.
